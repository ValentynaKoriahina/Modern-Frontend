import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

const CustomCircularProgress = ({ size = 24 }) => {
  return <CircularProgress size={size} />;
};

export default CustomCircularProgress;
